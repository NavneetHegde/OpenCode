mode: ContinuousDelivery

branches:
  main:
    regex: ^main
    tag: ""
  release:
    regex: ^release/.*
    tag: beta
    is-release-branch: true
  feature:
    regex: ^feature/.*
    tag: alpha
    increment: Inherit
    track-merge-target: false